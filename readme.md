# 🐹 Go 语言入门指南（新手友好版）

> 适合 **零基础 / 有其他语言基础 / 用于工作与后端开发** 的 Go（Golang）学习指南

---

## 1️⃣ Go 是什么？

**Go（Golang）** 是由 **Google** 设计并开源的一门现代编程语言，目标是：

> **简单、高性能、适合并发、适合工程化**

### Go 的核心特点

- ✅ 语法简单，学习成本低
- ✅ 编译速度快，部署方便（单一可执行文件）
- ✅ 原生支持高并发（goroutine）
- ✅ 非常适合：后端服务 / 微服务 / 云原生

---

## 2️⃣ Go 适合做什么？

Go 常见应用场景：

- 🌐 Web 后端服务（API / 微服务）
- ☁️ 云原生 / 容器 / Kubernetes
- ⚙️ 系统工具 / 命令行工具
- 🚀 高并发服务器（网关、消息系统）

### 使用 Go 的知名项目

- Docker
- Kubernetes
- Prometheus
- Etcd

---

## 3️⃣ Go vs 其他语言（快速对比）

| 语言 | 特点 | 是否推荐新手 |
|----|----|----|
| Go | 简单 + 高并发 + 工程化 | ⭐⭐⭐⭐⭐ |
| Java | 生态成熟但较复杂 | ⭐⭐⭐ |
| Python | 简单但性能较弱 | ⭐⭐⭐⭐ |
| Rust | 性能强但学习曲线陡 | ⭐⭐ |

---

## 4️⃣ Go 开发环境准备

### ① 安装 Go

官网：https://go.dev

安装完成后验证：

```bash
go version
```

---

### ② Go 环境变量（新手必知）

Go 1.18+ 默认使用 **Go Modules**，你只需要关心：

- `GOROOT`：Go 安装目录（通常自动配置）
- `GOPATH`：工作目录（可以不用管）

查看：

```bash
go env
```

---

## 5️⃣ 第一个 Go 程序（Hello World）

### 新建文件 `main.go`

```go
package main

import "fmt"

func main() {
    fmt.Println("Hello, Go!")
}
```

运行：

```bash
go run main.go
```

编译：

```bash
go build main.go
```

---

## 6️⃣ Go 程序结构讲解

```go
package main   // 包名（程序入口必须是 main）

import "fmt"  // 导入标准库

func main() {  // 主函数
    fmt.Println("Hello")
}
```

### 核心规则

- Go 程序从 `main.main()` 开始执行
- 文件名可以随意，但包名要一致

---

## 7️⃣ 基础语法（新手重点）

### ① 变量声明

```go
var a int = 10
var b = 20
c := 30   // 推荐写法
```

---

### ② 常量

```go
const Pi = 3.14
```

---

### ③ 基本数据类型

- `int`, `float64`
- `string`
- `bool`

```go
var name string = "Go"
var ok bool = true
```

---

## 8️⃣ 控制流程

### if

```go
if age > 18 {
    fmt.Println("adult")
} else {
    fmt.Println("child")
}
```

---

### for（Go 只有 for）

```go
for i := 0; i < 5; i++ {
    fmt.Println(i)
}
```

```go
for {
    // 无限循环
}
```

---

### switch

```go
switch day {
case 1:
    fmt.Println("Mon")
default:
    fmt.Println("Other")
}
```

---

## 9️⃣ 函数（非常重要）

```go
func add(a int, b int) int {
    return a + b
}
```

### 多返回值（Go 特色）

```go
func div(a, b int) (int, int) {
    return a / b, a % b
}
```

---

## 🔟 结构体（Struct）

```go
type User struct {
    Name string
    Age  int
}

u := User{Name: "Tom", Age: 18}
```

---

## 1️⃣1️⃣ 切片（Slice）

```go
arr := []int{1, 2, 3}
arr = append(arr, 4)
```

👉 Go 中 **90% 使用 slice 而不是数组**

---

## 1️⃣2️⃣ Map（字典）

```go
m := map[string]int{
    "a": 1,
    "b": 2,
}
```

---

## 1️⃣3️⃣ 接口（Interface）

```go
type Speaker interface {
    Speak() string
}
```

👉 **隐式实现接口**（Go 的核心设计）

---

## 1️⃣4️⃣ 并发（Go 的王牌）

### goroutine

```go
go func() {
    fmt.Println("run async")
}()
```

### channel

```go
ch := make(chan int)
ch <- 1
v := <-ch
```

👉 Go 并发 = **轻量 + 高效 + 易用**

---

## 1️⃣5️⃣ 错误处理（Go 风格）

```go
if err != nil {
    return err
}
```

👉 没有 try-catch，**显式处理错误**

---

## 1️⃣6️⃣ Go Modules（依赖管理）

初始化项目：

```bash
go mod init hello-go
```

下载依赖：

```bash
go get xxx
```

---

## 1️⃣7️⃣ 写一个最简单的 Web 服务

```go
package main

import (
    "net/http"
)

func main() {
    http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
        w.Write([]byte("Hello Go Web"))
    })

    http.ListenAndServe(":8080", nil)
}
```

访问：http://localhost:8080

---

## 1️⃣8️⃣ 新手常见误区

❌ 纠结语法细节
❌ 一开始就学底层源码
❌ 滥用指针

✅ 建议：
- 先会写
- 再写规范
- 最后追求性能

---

## 1️⃣9️⃣ 推荐学习路线

1. Go 基础语法
2. 标准库（fmt / net/http）
3. 并发模型
4. Web 框架（Gin / Echo）
5. 数据库
6. 微服务

---

## 🎯 总结一句话

> **Go = 简单语法 + 高并发 + 工程友好 + 后端王者**

非常适合：
- 后端开发
- 微服务
- 云原生方向
